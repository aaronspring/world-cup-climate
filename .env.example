# Arraylake auth. Easiest: run `uv run arraylake auth login` once (writes
# ~/.arraylake/token.json) and you don't need this file at all.
# For headless/CI, set a token instead:
# ARRAYLAKE_TOKEN=ema_xxx
